---
linux:  # Группа хостов
  children:  # Обозначение, что будет подгруппа хостов
    nginx_proxy:  # Имя подгруппы хостов для проксирующего сервера
      hosts:  # Узлы группы
        terraform1:  # Имя машины
          ansible_host: 51.250.72.79  # IP-адрес первой машины
    nginx_servers:  # Имя подгруппы хостов для отдающих серверов
      hosts:  # Узлы группы
        terraform2:  # Имя машины
          ansible_host: 84.201.132.156  # IP-адрес второй машины
        terraform3:  # Имя машины
          ansible_host: 158.160.63.206 # IP-адрес третьей машины
  vars:  # Переменные, доступные или используемые для всех подгрупп
    ansible_user: "madmax"  # Имя пользователя для подключения
    ansible_connection: ssh  # Тип подключения
    ansible_become: true  # Становиться ли другим пользователем после подключения
    ansible_become_user: "root"  # Пользователь, от имени которого будут выполняться команды (по умолчанию root)